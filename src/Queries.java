public class Queries {

    //      SELECT COUNT(`id`), Address, Age
    //      FROM `users`
    //      GROUP BY `address`
    //      HAVING COUNT(`id`) > 15;

    //      SELECT orders.id, users.name, user.surname
    //      FROM `orders`
    //      INNER JOIN `users` ON orders.userId = users.userId;

}
